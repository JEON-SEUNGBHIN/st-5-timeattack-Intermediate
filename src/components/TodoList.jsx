import TodoItem from "./TodoItem";

const TodoList = ({ isDone }) => {
  return (
    <section>
      <h2>Working...</h2>
      <ul>
        <TodoItem />
      </ul>
    </section>
  );
}
export default TodoList;
